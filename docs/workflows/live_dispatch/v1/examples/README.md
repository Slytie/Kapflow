# Live dispatch examples

These examples normalize the real day-of operational inputs:
- daily route-change emails,
- auto-accepted route-request notices,
- actual-hours carry-forward from EOS.

Added schedule-control bridge examples in this slice:
- service-day route-slot requirements (`dispatch.route_slot_requirements.workbook`),
- service-day capability matrix (`dispatch.driver_capabilities.workbook`),
- Stage02 input bundle (`dispatch.input_bundle.doc`),
- Stage02 candidate delta (`dispatch.candidate_schedule_delta.workbook`),
- Stage02 validation summary (`dispatch.validation_summary.doc`).
