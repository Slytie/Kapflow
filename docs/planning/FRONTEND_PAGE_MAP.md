# FRONTEND_PAGE_MAP.md

The inventory below lists the currently implemented routes first. EPIC-122 also freezes a future canonical workpage route posture under `/runs/:workflowRunId/workpages/*`; those planned routes are listed separately and are not implemented yet.

## Page inventory

| Page | Purpose | Primary user action | Primary data source | Cards/Rows | Detail model |
|---|---|---|---|---|---|
| `/demo/logistics` | Primary three-workflow operator demo shell plus logistics workpage launch point | Select family node, inspect node artifacts, choose run drill-down target, click unified-board task cards for authoritative task/artifact actions, and launch `Open EOD preview` or `Create editable EOD draft` from the backend-demo-workpages header | `GET /api/v1/stories/logistics-three-workflow` + `GET /api/v1/workflow-runs/{id}/workspace` + task detail/mutation routes via repositories + `POST /api/v1/workpages/demo/eod-v0/drafts` via `workpagesRepository` | Family graph + selected-node detail + embedded workflow-run graph + unified board + backend-demo-workpages header actions | Drawer-first task actions + optional composite-task `Expand process` subgraph + selected-node panel |
| `/demo/logistics/workpages/schedule-v0` | Full-page weekly planning review + selected-day preview workpage | Review the current planning week, inspect driver/day posture, and run bounded local what-if adjustments in UI state | `GET /api/v1/workpages/demo/schedule-v0` via `workpagesRepository.schedule()` over the backend demo workpage contract, with page-local freshness/source metadata and bounded local form state layered on top. | Summary cards + daily demand table + selected-day preview + driver roster + local what-if form | Full page (sibling route under `AppShell`, not drawer-first) |
| `/demo/logistics/workpages/eod-v0` | Full-page dispatch-reporting landing/preview workpage | Review the current EOD draft posture and create an editable artifact-backed draft | `GET /api/v1/workpages/demo/eod-v0` via `workpagesRepository.eod()` over the backend demo workpage contract, with page-local freshness/source metadata and read-only preview rendering. | Summary cards + route actuals table + closeout preview + checklist preview + warning panel + create-draft CTA | Full page (sibling route under `AppShell`, not drawer-first) |
| `/demo/logistics/workpages/eod-v0/artifacts/:artifactVersionId` | Full-page artifact-backed EOD draft/review workpage with bounded recent-version history | Complete closeout fields, select/update UPD candidates, inspect recent draft lineage, submit a new immutable workbook version, and reopen adjacent/latest drafts on conflict or staleness | `GET /api/v1/workpages/artifacts/{artifact_version_id}`, `POST /api/v1/workpages/demo/eod-v0/drafts`, `POST /api/v1/workpages/artifacts/{artifact_version_id}/submit`, and `GET /api/v1/workflow-runs/{workflow_run_id}/artifacts` via `workpagesRepository`, with page-local edit state layered over the artifact-backed contract plus download/history affordances. | Summary cards + route actuals table + editable closeout form + editable checklist + warning panel + conflict/download/lineage panels + recent draft versions panel | Full page (sibling route under `AppShell`, not drawer-first) |
| `/board` | Legacy schedule-only triage/regression view | Open task pane for task transitions; run approval/flag actions in-lane | `GET /api/v1/board/schedule-planning` + `GET /api/v1/flags` via `boardRepository` | Board cards (`TaskCardWide`, `ApprovalCard`, `FlagCard`) | Drawer |
| `/my-work` | Secondary assigned queue for operator execution | Open task pane for claim/complete; keep attachment actions inline | `GET /api/v1/human-tasks` via `humanTasksRepository` | Rows (`QueueRow`) | Drawer (simple task detail/actions; optional composite-task process expansion) |
| `/approvals` | Approval queue with review workspace | Approve/reject/request changes | `GET /api/v1/approvals` + `POST /api/v1/approvals/{id}/respond` + run detail artifacts | Cards + split review pane | Split-pane + drawer |
| `/exceptions` | Exception/flag queue | Inspect severity and related run context | `GET /api/v1/flags` via `flagsRepository` | Cards (`FlagCard`) | Drawer |
| `/runs` | Supporting run list for drill-down | Open run detail | `GET /api/v1/workflow-runs` via `workflowRunsRepository` | Rows (`QueueRow`) | Drawer + detail page |
| `/runs/:workflowRunId/workspace` | Single-run operator workspace with synchronized graph + actions | Execute inline work while monitoring run progression | `GET /api/v1/workflow-runs/{id}/workspace` + existing task/approval/artifact mutation routes via repositories | Graph + cards (`TaskCardWide`, `ApprovalCard`, `FlagCard`) | Drawer (+ optional composite-task process expansion when available) |
| `/runs/:workflowRunId` | Full run inspection across timeline/tasks/approvals/artifacts/exceptions | Tabbed inspection | `GET /api/v1/workflow-runs/{id}` + `GET /api/v1/timeline-events` | Rows and tab lists | Page tabs + drawer hook |
| `/official-outputs` | Pointer/current official output visibility | Inspect pointer promotion metadata | `GET /api/v1/pointers` via `pointersRepository` | Cards (`PointerCard`) | Drawer |
| `/timeline` | Legacy schedule-oriented event stream explorer | Filter and inspect event details | `GET /api/v1/timeline-events` via `timelineRepository` | Rows (`TimelineRow`) | Drawer |

## EPIC-122 Canonical Route Posture (Frozen, Not Yet Implemented)

| Page | Purpose | Primary user action | Primary data source | Cards/Rows | Detail model |
|---|---|---|---|---|---|
| `/runs/:workflowRunId/workpages/schedule-v0` | Planned canonical weekly-planning run-backed review workpage | Open a schedule review page that is explicitly tied to one `weekly_schedule_planning.v1` run while keeping the page query-backed/composite | `GET /api/v1/workpages/workflow-runs/{workflow_run_id}/schedule-v0` via a future run-backed `workpagesRepository` method; `/demo/logistics/workpages/schedule-v0` remains a curated alias until this route is proven. | Same weekly review sections already validated for the current workpage page | Full page under `AppShell` |
| `/runs/:workflowRunId/workpages/eod-v0` | Planned canonical dispatch-reporting run-backed landing/draft-resolution workpage | Open the EOD landing for one `dispatch_reporting.v1` run, see whether a latest editable draft already exists, and either create or reopen that draft | `GET /api/v1/workpages/workflow-runs/{workflow_run_id}/eod-v0` plus `POST /api/v1/workpages/workflow-runs/{workflow_run_id}/eod-v0/drafts`; the response adds `run_context` plus EOD-only `draft_resolution`, while `/demo/logistics/workpages/eod-v0` remains a curated alias until this route is proven. | Same landing/preview sections already validated for the current EOD landing page | Full page under `AppShell` |
| `/runs/:workflowRunId/workpages/eod-v0/artifacts/:artifactVersionId` | Planned canonical frontend route for artifact-backed EOD editing under a workflow-run workpage family | Continue editing through the existing immutable workbook-backed EOD route after the run-backed landing resolves or creates the latest draft | Backend artifact APIs stay unchanged: `GET /api/v1/workpages/artifacts/{artifact_version_id}`, `POST /api/v1/workpages/artifacts/{artifact_version_id}/submit`, and `GET /api/v1/workflow-runs/{workflow_run_id}/artifacts`; this nested frontend route is the canonical post-EPIC-122 posture, while `/demo/logistics/workpages/eod-v0/artifacts/:artifactVersionId` remains a curated alias during migration. | Same artifact-backed EOD sections already validated for the current page | Full page under `AppShell` |
