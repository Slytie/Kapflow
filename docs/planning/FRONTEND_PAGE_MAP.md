# FRONTEND_PAGE_MAP.md

The inventory below lists the active implemented routes. Workpage posture is canonical-only: `/` is the shared-env-safe operator home, `/demo/logistics` remains the logistics demo shell, and all workpage pages now live under `/runs/:workflowRunId/workpages/*`.

## Page inventory

| Page | Purpose | Primary user action | Primary data source | Cards/Rows | Detail model |
|---|---|---|---|---|---|
| `/` | Operator home and failure-state posture surface | Inspect current server-derived session posture and dry-run reconciler findings before opening work queues or demo flows | `GET /api/v1/operator/home` plus viewer bootstrap | Runtime posture cards + reconciler finding groups | Full page under `AppShell` |
| `/demo/logistics` | Primary three-workflow logistics shell plus canonical workpage discovery point | Select workflow-family context, inspect linked runs, and launch canonical workpage pages for weekly planning and dispatch reporting | `GET /api/v1/stories/logistics-three-workflow` plus run/workspace/task detail endpoints | Family graph + selected-node detail + unified board + canonical workpage launch actions | Drawer-first task actions + selected-node panel |
| `/runs/:workflowRunId/workpages/schedule-v0` | Canonical weekly-planning landing page | Open the current weekly schedule surface for one run and follow backend handoff actions to the latest draft, route-demand, or driver-preferences surfaces | `GET /api/v1/workpages/workflow-runs/{workflow_run_id}/schedule-v0` | Summary cards + schedule heatmap + dependency/check header status + selected-day detail | Full page under `AppShell` |
| `/runs/:workflowRunId/workpages/schedule-v0/artifacts/:artifactVersionId` | Canonical artifact-backed schedule editor/history page | Edit bounded reassignment/on-call rows, review or apply route-demand coverage recommendations when present, preview recalculation, and save a superseding draft | `GET /api/v1/workpages/workflow-runs/{workflow_run_id}/schedule-v0/artifacts/{artifact_version_id}` + schedule preview/save + route-demand coverage recommend/apply + workflow-run artifact history | Coverage recommendation panel + schedule surface + heatmap editor + advanced artifact info | Full page under `AppShell` |
| `/runs/:workflowRunId/workpages/route-demand-v0` | Canonical route-demand landing page | Open the latest route-demand artifact for the run | `GET /api/v1/workpages/workflow-runs/{workflow_run_id}/route-demand-v0` | Summary cards + day cards + schedule-impact banner | Full page under `AppShell` |
| `/runs/:workflowRunId/workpages/route-demand-v0/artifacts/:artifactVersionId` | Canonical artifact-backed route-demand editor/history page | Adjust day-level planned route counts, save a new immutable successor artifact, and hand off either to existing-week coverage or future-week scheduling activation | `GET /api/v1/workpages/workflow-runs/{workflow_run_id}/route-demand-v0/artifacts/{artifact_version_id}` + submit + save-and-run + workflow-run artifact history | Day-card editor + schedule-impact banner + immutable history rail | Full page under `AppShell` |
| `/runs/:workflowRunId/workpages/driver-preferences-v0` | Canonical driver-preferences landing page | Open the latest weekly snapshot or create the first snapshot for the run | `GET /api/v1/workpages/workflow-runs/{workflow_run_id}/driver-preferences-v0` + `POST /api/v1/workpages/workflow-runs/{workflow_run_id}/driver-preferences-v0/snapshots` | Explanatory landing copy + schedule-impact summary + create/open CTA | Full page under `AppShell` |
| `/runs/:workflowRunId/workpages/driver-preferences-v0/artifacts/:artifactVersionId` | Canonical artifact-backed driver-preferences editor/history page | Edit the weekly Sunday-Saturday preference grid and save a new immutable snapshot successor | `GET /api/v1/workpages/workflow-runs/{workflow_run_id}/driver-preferences-v0/artifacts/{artifact_version_id}` + submit + workflow-run artifact history | Preference grid + schedule-impact banner + immutable history rail | Full page under `AppShell` |
| `/runs/:workflowRunId/workpages/eod-v0` | Canonical dispatch-reporting landing page | Open the EOD landing for one run, then create or reopen the latest draft through backend `draft_resolution` | `GET /api/v1/workpages/workflow-runs/{workflow_run_id}/eod-v0` + `POST /api/v1/workpages/workflow-runs/{workflow_run_id}/eod-v0/drafts` | Summary cards + route actuals preview + closeout preview + checklist preview + warning panel | Full page under `AppShell` |
| `/runs/:workflowRunId/workpages/eod-v0/artifacts/:artifactVersionId` | Canonical artifact-backed EOD editor/history page | Complete closeout fields, edit UPD candidates, submit a new immutable workbook version, and reopen recent/latest drafts | `GET /api/v1/workpages/workflow-runs/{workflow_run_id}/eod-v0/artifacts/{artifact_version_id}` + submit + workflow-run artifact history | Summary cards + route actuals table + closeout form + checklist + warning panel + recent draft versions rail | Full page under `AppShell` |
| `/runs/:workflowRunId/workspace` | Single-run operator workspace with synchronized graph + backend-projected workpage actions | Execute inline work while monitoring run progression and open/create workpages through backend-projected action metadata | `GET /api/v1/workflow-runs/{id}/workspace` plus existing task/approval/artifact mutation routes | Graph + cards (`TaskCardWide`, `ApprovalCard`, `FlagCard`) | Drawer (+ optional composite-task process expansion) |
| `/board` | Legacy schedule-only triage/regression view | Open task pane for task transitions; run approval/flag actions in-lane | `GET /api/v1/board/schedule-planning` + `GET /api/v1/flags` | Board cards (`TaskCardWide`, `ApprovalCard`, `FlagCard`) | Drawer |
| `/my-work` | Secondary assigned queue for operator execution | Open task pane for claim/complete; keep attachment actions inline | `GET /api/v1/human-tasks` | Rows (`QueueRow`) | Drawer |
| `/approvals` | Approval queue with review workspace | Approve/reject/request changes | `GET /api/v1/approvals` + `POST /api/v1/approvals/{id}/respond` + run detail artifacts | Cards + split review pane | Split-pane + drawer |
| `/exceptions` | Exception/flag queue | Inspect severity and related run context | `GET /api/v1/flags` | Cards (`FlagCard`) | Drawer |
| `/runs` | Supporting run list for drill-down | Open run detail | `GET /api/v1/workflow-runs` | Rows (`QueueRow`) | Drawer + detail page |
| `/runs/:workflowRunId` | Full run inspection across timeline/tasks/approvals/artifacts/exceptions | Tabbed inspection | `GET /api/v1/workflow-runs/{id}` + `GET /api/v1/timeline-events` | Rows and tab lists | Page tabs + drawer hook |
| `/official-outputs` | Pointer/current official output visibility | Inspect pointer promotion metadata | `GET /api/v1/pointers` | Cards (`PointerCard`) | Drawer |
| `/timeline` | Legacy schedule-oriented event stream explorer | Filter and inspect event details | `GET /api/v1/timeline-events` | Rows (`TimelineRow`) | Drawer |

## Frozen notes
- Workpage discovery starts from `/demo/logistics` and run/workspace surfaces, not from nested demo workpage routes.
- Active workspace action presentation is `open_route | create_then_open`.
- `schedule-v0` stays on weekly reassignment/recalculation semantics.
- `route-demand-v0` owns demand truth edits separately from `schedule-v0`, but existing-week positive increases can hand off into `schedule-v0` backend coverage recommendations.
- `driver-preferences-v0` is advisory only.
