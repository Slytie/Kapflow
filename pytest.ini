[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    capex_semantic: CAPEX semantic safety, review-gate, and governance tests tracked by the CB2 semantic backlog.
    logistics_regression: logistics-domain regression tests that are intentionally excluded from platform-substrate-only lanes.
