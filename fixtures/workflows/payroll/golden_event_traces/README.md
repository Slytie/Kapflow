# Golden event traces (placeholder)

This folder is intentionally still a placeholder during the Schedule Planning-first runtime wedge.

Do **not** treat Payroll as the default replay-ready behavioral corpus yet.
Today Payroll is still used primarily as:
- a complete authored workflow pack,
- a linear approval-heavy governance reference,
- a semantic cross-check for shared substrate decisions.

This folder will later hold JSONL or JSON fixtures that describe the expected TimelineEvent sequence
for key scenarios:
- payroll happy path
- drift after review
- degraded audit export/index
- isolation violations denied

Populate as part of EPIC-090 once runtime work reaches the Payroll reference wedge.
